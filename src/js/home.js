import displayTask from "./displayTask";

const stateContent = document.querySelector('.state-content')
const homeContainer = document.querySelector('.homw')

const setHome  = (tasks) => {
    stateContent.style.display = 'none'
    
}

export default setHome